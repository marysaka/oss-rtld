%rename link                old_link

*link:
%(old_link) -T %:getenv(TOPDIR /misc/rtld.ld) -shared --discard-all --strip-all --version-script=%:getenv(TOPDIR /exported.txt) -init=__rtld_init -fini=__rtld_fini --gc-sections -z text --build-id=sha1

*startfile:
crti%O%s crtbegin%O%s